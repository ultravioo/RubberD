<%= simple_form_for [ @duck, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (1..5).to_a, label: "Rating" %>
  <%= f.submit "Submit review", class: "btn btn-warning" %>
<% end %>
